let x = 24.1985349;
let y = 55;
let z;

// z = Math.floor(x) this is use to floor the large floating point number
// z = Math.random(x) this is use to generate randon number below the given number
// z = Math.max(4, 8); this is use to find the max value of two number
// z = Math.round(x); this is use to round the floating point number
// z = Math.ceil(x); this is use to increase any number by one
// z = Math.pow(x, y); this is use to find the pow of any number
z = Math.trunc(x);

console.log(z);