// Spread Operator = ... allows an iterable as an array or string 
// to be expanded into seperate elements (unpack the elements)

let numbers = [1, 2, 3, 4, 5];

let maximun = Math.max(...numbers);

console.log(maximun);
